import{createFileRoute}from"@tanstack/react-router";import{useQuery}from"@tanstack/react-query";import{miscApi}from"@/api/services";import{EmptyState,PageSkeleton}from"@/components/shared/page-state";
export const Route=createFileRoute("/combos")({head:()=>({meta:[{title:"Plant Combos | Plant Nursery"},{name:"description",content:"Curated plant combinations for thoughtful green spaces."},{property:"og:title",content:"Plant Combos | Plant Nursery"},{property:"og:description",content:"Curated plant combinations."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const q=useQuery({queryKey:["combos"],queryFn:miscApi.combos});if(q.isLoading)return <PageSkeleton/>;return <div className="mx-auto max-w-[1480px] px-6 py-12 lg:px-10"><h1 className="font-display text-4xl">Curated plant combos</h1>{q.data?.length?<div className="mt-8 grid gap-5 md:grid-cols-3">{q.data.map(c=><article key={c.id} className="border p-6"><h2 className="font-display text-2xl">{c.name}</h2><p className="mt-3 text-sm text-muted-foreground">{c.description}</p>{c.price&&<p className="mt-5 font-bold">₹{c.price.toLocaleString('en-IN')}</p>}</article>)}</div>:<EmptyState title="New combinations are taking root" description="Curated plant sets will appear here when the nursery publishes them."/>}</div>}
