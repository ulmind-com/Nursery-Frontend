import { createFileRoute } from "@tanstack/react-router"; import { CataloguePage } from "@/components/product/catalogue-page";
export const Route=createFileRoute("/category/$slug")({head:({params})=>({meta:[{title:`${params.slug} | Plant Nursery`},{name:"description",content:`Shop the ${params.slug} collection.`},{property:"og:title",content:`${params.slug} | Plant Nursery`},{property:"og:description",content:`Shop the ${params.slug} collection.`},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:()=>{const {slug}=Route.useParams();return <CataloguePage title={slug.replaceAll("-"," ")} params={{category_id:slug}}/>}});
