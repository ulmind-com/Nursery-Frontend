import {createFileRoute,Link,useNavigate}from"@tanstack/react-router";import{useState}from"react";import{toast}from"sonner";import{authApi}from"@/api/services";import{useAuth}from"@/contexts/auth-context";import{normalizeApiError}from"@/lib/api";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";
export const Route=createFileRoute("/login")({head:()=>({meta:[{title:"Sign In | Plant Nursery"},{name:"description",content:"Sign in to manage your orders, addresses, and wishlist."},{property:"og:title",content:"Sign In | Plant Nursery"},{property:"og:description",content:"Access your Plant Nursery account."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Login});function Login(){const[loading,setLoading]=useState(false),{acceptAuth}=useAuth(),nav=useNavigate();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);try{acceptAuth(await authApi.login({email:String(f.get("email")),password:String(f.get("password"))}));toast.success("Welcome back");await nav({to:"/account"})}catch(err){toast.error(normalizeApiError(err).message)}finally{setLoading(false)}}return <AuthShell title="Welcome back" note="Sign in to see your orders, saved plants, and addresses."><form onSubmit={submit} className="space-y-5"><div><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" required className="mt-2"/></div><div><Label htmlFor="password">Password</Label><Input id="password" name="password" type="password" required minLength={6} className="mt-2"/></div><Button disabled={loading} className="w-full" size="lg">{loading?"Signing in...":"Sign in"}</Button><p className="text-center text-sm text-muted-foreground">New here? <Link to="/signup" className="font-semibold text-primary">Create an account</Link></p></form></AuthShell>};
export function AuthShell({title,note,children}:{title:string;note:string;children:React.ReactNode}){return <div className="mx-auto grid min-h-[70vh] max-w-5xl place-items-center px-6 py-14"><div className="w-full max-w-md"><p className="text-xs font-bold uppercase text-primary">Your nursery account</p><h1 className="mt-2 font-display text-4xl">{title}</h1><p className="mb-8 mt-3 text-sm leading-6 text-muted-foreground">{note}</p>{children}</div></div>}
