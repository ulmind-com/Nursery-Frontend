import{createFileRoute,Link}from"@tanstack/react-router";import{useState}from"react";import{toast}from"sonner";import{authApi}from"@/api/services";import{useAuth}from"@/contexts/auth-context";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{EmptyState}from"@/components/shared/page-state";
export const Route=createFileRoute("/account/profile")({head:()=>({meta:[{title:"Profile | MyGarden"},{name:"description",content:"Update your profile details."},{property:"og:title",content:"Profile | MyGarden"},{property:"og:description",content:"Update your profile details."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Profile});function Profile(){const{user,refreshUser}=useAuth();const[busy,setBusy]=useState(false);if(!user)return <EmptyState title="Please sign in" description="Sign in to update your profile." action={<Button asChild><Link to="/login">Sign in</Link></Button>}/>;return <div className="mx-auto max-w-xl px-6 py-12"><h1 className="font-display text-4xl">Your profile</h1><form className="mt-8 space-y-5" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await authApi.update({name:String(f.get("name")),phone:String(f.get("phone"))});await refreshUser();toast.success("Profile updated")}finally{setBusy(false)}}}><div><Label>Name</Label><Input name="name" defaultValue={user.name} className="mt-2"/></div><div><Label>Email</Label><Input value={user.email} disabled className="mt-2"/></div><div><Label>Phone</Label><Input name="phone" defaultValue={user.phone||""} className="mt-2"/></div><Button disabled={busy}>Save changes</Button></form></div>}
