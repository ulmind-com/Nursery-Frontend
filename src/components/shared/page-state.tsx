import { Leaf, RefreshCw } from "lucide-react";
import { Button } from "@/components/ui/button";
export function EmptyState({ title, description, action }: { title: string; description: string; action?: React.ReactNode }) { return <div className="mx-auto flex max-w-lg flex-col items-center px-6 py-24 text-center"><span className="mb-5 flex size-14 items-center justify-center rounded-full bg-accent text-primary"><Leaf /></span><h2 className="font-display text-3xl">{title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{description}</p>{action && <div className="mt-7">{action}</div>}</div>; }
export function ErrorState({ retry }: { retry: () => void }) { return <EmptyState title="Something went wrong" description="Unable to connect to the nursery service. Please try again." action={<Button onClick={retry}><RefreshCw />Try again</Button>} />; }
export function PageSkeleton() { return <div className="mx-auto max-w-[1480px] animate-pulse px-6 py-12"><div className="h-9 w-64 rounded bg-muted"/><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{Array.from({length:8}).map((_,i)=><div key={i}><div className="aspect-[4/5] rounded bg-muted"/><div className="mt-3 h-4 w-2/3 rounded bg-muted"/><div className="mt-2 h-4 w-1/3 rounded bg-muted"/></div>)}</div></div>; }